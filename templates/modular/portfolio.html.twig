<section id="{% if page.header.redirect %}{{ page.header.redirect }}{% else %}{{ page.menu|lower|replace({' ': '-'})}}{% endif %}" class="section portfolio py-4 py-md-6">
  <div class="container">
      <h2 class="title mb-4">{{page.title|capitalize }}</h2>
    <div class="row">
      <div class="col text-center">
        <p>Rien à afficher pour l'instant.</p>
      </div>
    </div>
  </div>
</section>
