<section id="{% if page.header.redirect %}{{ page.header.redirect }}{% else %}{{ page.menu|lower|replace({' ': '-'})}}{% endif %}" class="section portfolio py-6">
  <div class="container">
    <div class="section__title mb-4">
      <h2 class="title">{{page.title|capitalize }}</h2>
    </div>
    <div class="row">
      <div class="col text-center">
        <p>Rien à afficher pour l'instant.</p>
      </div>
    </div>
  </div>
</section>
