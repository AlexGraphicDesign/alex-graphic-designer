<section id="{% if page.header.redirect %}{{ page.header.redirect }}{% else %}{{ page.menu|lower|replace({' ': '-'})}}{% endif %}" class="section portfolio">
  <div class="section__title title__vertical">
    <h2 class="title">{{page.title|capitalize }}</h2>
  </div>
  <div class="container">
    <div class="row align-items-center">
    </div>
  </div>
</section>
