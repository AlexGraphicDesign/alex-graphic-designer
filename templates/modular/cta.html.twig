<section id="{% if page.header.redirect %}{{ page.header.redirect }}{% else %}{{ page.menu|lower|replace({' ': '-'})}}{% endif %}" class="section cta py-5" style="background-image:url({{ url('theme://images/default.jpg') }});">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-10">
        <div class="card">
          <h2 class="title">Call to Action</h2>
        </div>
      </div>
    </div>
  </div>
</section>
