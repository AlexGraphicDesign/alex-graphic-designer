{% if page.taxonomy.tag %}
  {% for tag in page.taxonomy.tag %}
    <a class="badge badge-pill badge-primary {{ label_style ?: 'label-secondary' }}" href="{{ blog.url|rtrim('/') }}/tag{{ config.system.param_sep }}{{ tag }}#main-content">{{ tag }}</a>
  {% endfor %}
{% endif %}
