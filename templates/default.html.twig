{% extends 'partials/base.html.twig' %}

{% block content %}
<!-- Contenu principal ================================================== -->
  <section class="" style="background-image:url({{ url('theme://images/default.jpg') }}">
      <div class="container">
        <div class="row text-center">
            <h1>
                {{ page.title }}
            </h1>
        </div>
      </div>
  </section>

  <section>
    <div class="container">
      <div class="row">
        <div class="col">

          {{ page.content }}

        </div>
      </div>
    </div>
  </section>
{% endblock %}
